# Dooray MCP Server 에이전트 지침

이 저장소는 Dooray 공식 API를 독립적으로 호출하는 Kotlin 기반 MCP 서버다.
에이전트는 안전하고 명확한 작업을 사용자 승인 없이 끝까지 수행하고, 완료를 주장하기 전에 검증 근거를 남긴다.

## 자율 실행 원칙

- 명확하고 되돌릴 수 있는 조사, 수정, 테스트, 검토는 계속 진행한다.
- 자격 증명이 필요하거나, 되돌릴 수 없는 외부 작업이거나, 제품 범위가 달라지는 결정만 사용자에게 묻는다.
- 작업 트리에 있는 사용자 변경과 다른 작업자의 변경을 보존한다.
- 파괴적인 Git 명령과 실제 Dooray 삭제 API를 실행하지 않는다.
- 외부에 게시할 GitHub 본문, Dooray 댓글, 메일, 릴리스 노트는 등록 전에 `content-preview` 절차로 보여준다.

## 작업 전 확인

1. 현재 브랜치, 작업 트리 상태, 대상 파일을 확인한다.
2. 요청에 적용되는 `SKILL.md`가 있으면 전체를 읽고 따른다.
3. 제품 계약과 설계는 다음 문서를 우선한다.

   - `docs/prd.md`
   - `docs/flow.md`
   - `docs/code-architecture.md`
   - `docs/data-schema.md`
   - `docs/parity/dooray-cli-v0.16.0.md`
   - `docs/adr/INDEX.md`

4. 구현 전에 관련 작업 명세를 `tasks/<plan>/`에서 확인한다.

## 지침 단일 원본과 에이전트 하네스

- 저장소의 공통 에이전트 지침은 `AGENTS.md` 하나만 원본으로 관리한다.
- `CLAUDE.md`는 `AGENTS.md`를 가리키는 상대 심볼릭 링크로 유지한다.
- 공통 규칙을 바꿀 때는 `AGENTS.md`만 수정하고 `CLAUDE.md`에 별도 내용을 만들지 않는다.
- Claude 전용 명령과 역할별 보조 지침은 다음 경로에 둔다.

  - `.claude/commands/`
  - `.claude/planning-overlay.md`
  - `.claude/build-with-teams-overlay.md`
  - `.claude/agents/dooray-mcp-docs-verifier.md`

- 다른 코딩 에이전트가 `AGENTS.md`를 직접 읽거나 `CLAUDE.md`를 자동으로 읽어도 같은 공통 계약을 적용해야 한다.
- 에이전트별 보조 지침은 공통 계약을 반복하지 않고 해당 실행 환경에만 필요한 차이만 기록한다.

## 독립 구현 경계

MCP 서버는 `dooray-cli`의 동작을 참고하되 코드와 실행 환경에서 완전히 독립적이어야 한다.

금지 사항:

- `dooray` 프로세스 실행
- `@bifos/dooray-cli` 의존성 추가
- CLI 소스나 내부 모듈 가져오기
- Docker 이미지에 CLI 설치
- 빌드나 배포에서 CLI 저장소 요구
- CLI 명령을 감싸는 변환 계층 구현

허용 사항:

- CLI v0.16.0의 코드, 테스트, 문서, ADR을 검증된 동작의 참고 자료로 읽기
- CLI의 안전 정책, API 특이점, 오류 처리를 Kotlin으로 독립 구현하기
- Dooray 공식 API 문서를 외부 계약의 최종 근거로 사용하기

## 도구 공개 정책

- 목표 프로필은 `compact` 12개, `legacy` 20개, `all` 32개다.
- 장기 기본값은 `compact`다.
- 기존 20개 도구는 `legacy`와 `all` 프로필에서 호환성을 유지한다.
- 기능 수만큼 도구를 늘리지 않는다. 자원별 도구와 `operation` 파라미터로 관련 기능을 묶는다.
- 위험한 변경은 `dry_run`, `request_id`, 삭제 준비·확정 계약을 적용한다.
- 구조화 결과를 기본으로 하고, 호환용 `TextContent`를 함께 제공한다.

## 코드 구조와 책임

- `client/`: Dooray HTTP 계약과 전송
- `config/`: 환경 변수, 비밀 파일 참조, 실행 설정
- `domain/`: 프로젝트, 업무, 위키, 메일, 메신저의 도메인 모델과 규칙
- `resolver/`: 이름, 이메일, ID, URL, 프로젝트 코드 해석
- `service/`: 페이지 처리, 캐시, 파일 전송, 삭제 확인, 멱등성
- `tools/`: MCP 입력 검증과 도메인 서비스 호출
- `resources/`: 파일과 진단 정보를 MCP 리소스로 제공
- `types/`: 외부 응답과 MCP 구조화 결과 모델

새 기능은 도구에서 HTTP를 직접 호출하지 않는다.
도구 계층은 입력 검증과 결과 변환만 담당하고, API 호출은 클라이언트나 도메인 서비스에 둔다.

## 보안과 안전

- API 키, 메일 비밀번호, 실제 ID, 사내 도메인, 이메일, 실명을 코드·테스트·로그·문서에 넣지 않는다.
- 인증 정보 원문을 도구 결과로 반환하지 않는다.
- 쓰기 작업은 자동 재시도하지 않는다.
- 파일 경로 입력은 기본 비활성화하고, 허용된 루트 내부만 처리한다.
- Docker 경계는 `/data/in`, `/data/out`, `/data/state`, `/run/secrets`를 사용한다.
- 삭제는 5분 유효 확인 토큰을 사용하는 준비·확정 두 단계로 수행한다.
- 토큰이 없거나, 만료됐거나, 대상과 영향 정보가 달라지면 삭제하지 않는다.

## 구현 방식

- 기존 유틸리티와 패턴을 우선하며 새 의존성과 추상화는 근거가 있을 때만 추가한다.
- 관심사별로 작은 커밋을 만든다.
- 독립 구현 묶음은 별도 브랜치와 별도 작업 트리에서 진행한다.
- 공유 문서나 공통 등록 파일은 한 작업자만 소유한다.
- 구현 작성자와 최종 검토자를 가능한 한 분리한다.
- 외부 기여 PR은 원 작성자의 커밋과 귀속을 가능한 범위에서 보존한다.

## 검증 명령

기본 검증:

```bash
CI=true ./gradlew clean build --no-daemon --console=plain
./gradlew testMcpIntegration --no-daemon --console=plain
git diff --check
```

실제 Dooray 자격 증명이 필요한 통합 테스트는 명시적으로 분리한다.
CI나 일반 검증에서 실제 삭제 API와 외부 사용자에게 영향을 주는 호출을 실행하지 않는다.

변경 범위에 따라 다음 근거를 추가한다.

- 도구 목록 스냅숏과 모든 입력 스키마
- Dooray API 요청·응답 계약
- resolver의 정확 일치, 부분 일치, 모호성
- 업무 참여자만 수정할 때 기존 필드 보존
- 파일 업로드·다운로드와 Docker 경계
- 삭제 토큰 만료와 대상 불일치
- 캐시와 전체 페이지 처리
- 구조화된 오류 코드와 재시도 가능 여부
- 메일과 메신저 오류 분류
- `linux/amd64`, `linux/arm64` 이미지 빌드와 MCP 연결

## 문서와 언어

- 사용자 문서, 저장소 지침, 스킬 설명은 자연스러운 한국어로 작성한다.
- 명령어, 경로, API 필드, 클래스명, `agent_type`, `$workflow` 같은 기계 계약은 원문을 유지한다.
- 기능을 조용히 제외하지 않는다.
- CLI 전용 상호작용은 MCP에서 같은 사용자 결과를 내는 방식으로 재설계하고 기능 대응표에 근거를 기록한다.
- 문서가 코드와 다르면 같은 변경에서 함께 고친다.

## 병합과 배포

- PR의 일반 댓글, 리뷰, 인라인 댓글과 미해결 스레드를 모두 확인한다.
- 로컬 검증과 GitHub CI가 통과한 뒤 저장소의 기존 병합 방식을 사용한다.
- 시험 이미지는 안정 태그와 `latest`보다 먼저 배포한다.
- 안정 배포 전에는 시험 태그를 실제 MCP 클라이언트에서 검증한다.
- 배포에는 GitHub Release, Docker 다중 아키텍처 태그, pull 명령 검증이 포함된다.
- 병합 완료된 작업 트리만 제거하고, 미병합 작업 트리는 보존한다.
